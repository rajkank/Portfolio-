# Resend — contact form (server only; never expose RESEND_API_KEY to the client)
# Get a key at https://resend.com/api-keys
# Restart `npm run dev` after changing these values.

RESEND_API_KEY=
CONTACT_EMAIL=

# Optional. Until you verify a domain, Resend test sending uses:
# RESEND_FROM_EMAIL=Raj Sudhir Kank <onboarding@resend.dev>
